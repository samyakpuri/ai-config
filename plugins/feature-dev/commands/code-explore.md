---
description: Deeply explore a codebase area — traces execution paths, maps architecture layers, identifies patterns and dependencies
argument-hint: <feature, area, or component to explore>
---

Spawn a `feature-dev:code-explorer` agent with this task:

"Explore and analyse: $ARGUMENTS

Trace execution paths, map architecture layers, identify patterns, abstractions, and dependencies. Return a list of 5–10 essential files for understanding this area."

After the agent completes, read all the key files it surfaces, then present a comprehensive summary of findings.
